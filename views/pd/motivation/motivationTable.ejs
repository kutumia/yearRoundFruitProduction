<%if (records.length >0){ %>
    <table class="table table-bordered table-striped" id="tblCustomers" cellspacing="0" cellpadding="0">
    <thead>
    <tr>
        <th>কৃষকের নাম</th>
        <th>ঠিকানা</th>
        <th>মোবাইল নং</th>
        <th>স্থাপিত বাগানের ধরণ</th>
        <th>বাগানে জমির পরিমাণ</th>
        <th>মন্তব্য</th>
        <th>পরিবর্তন/মুছুন</th>
    </tr>
    </thead>
    <tbody>
        <% records.forEach(function(row){ %>
            <tr>
                <td><%= row.farmer %></td>
                <td><%= row.address %></td>
                <td><%= row.mobile %></td>
                <td><%= row.bagan %></td>
                <td><%= row.jomi %></td>
                <td><%= row.comment %></td>
                <td><a class="btn btn-primary btn-xs" href="/edit/<%= row.id %>"><i class="fa fa-edit"></i></a>
                    <a class="btn btn-danger btn-xs" href="/delete/<%= row.id %>"><i class="fa fa-trash"></i></a>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
<% }else{ %>
    <table class="table table-bordered table-striped" id="tblCustomers" cellspacing="0" cellpadding="0">
        <thead>
        <tr>
            <th>কৃষকের নাম</th>
            <th>ঠিকানা</th>
            <th>মোবাইল নং</th>
            <th>স্থাপিত বাগানের ধরণ</th>
            <th>বাগানে জমির পরিমাণ</th>
            <th>মন্তব্য</th>
            <th>পরিবর্তন/মুছুন</th>
        </tr>
        </thead>
        <tbody>
        <% records.forEach(function(row){ %>
            <tr>
                <td colspan="10">No data Found</td>
            </tr>
        <% }) %>
        </tbody>
    </table>
<% } %>